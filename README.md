# Bitburner scripts
Just a collection of bitburner scripts which I have written and / or modified.

Attribution: `getServers.js`, `solve-contracts.js`, and `gang.js` are based on scripts from `git@github.com:kamukrass/Bitburner.git`, with local modifications.

